% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pena_strawderman_hollander.R
\name{psh_surv}
\alias{psh_surv}
\title{Pena--Strawderman--Hollander recurrent survival estimator}
\usage{
psh_surv(
  data,
  id = "id",
  gap = "gap",
  status = "status",
  episode = NULL,
  tau = Inf
)
}
\arguments{
\item{data}{A data frame in long gap-time format with one row per observed
gap.}

\item{id}{Character scalar naming the subject identifier column.}

\item{gap}{Character scalar naming the gap-duration column.}

\item{status}{Character scalar naming the event indicator column.}

\item{episode}{Optional character scalar naming the within-subject episode
order column.}

\item{tau}{Optional upper truncation time for the recurrent gap distribution.}
}
\value{
A data frame with one row per distinct completed gap time and columns
\code{time}, \code{risk}, \code{dN}, \code{dLambda}, \code{surv_left}, and \code{surv}.
}
\description{
Estimates the recurrent gap-time survival distribution using the generalized
product-limit estimator of Pena, Strawderman, and Hollander (PSH). With the
long gap-time data convention used in this package, PSH is the ordinary
Kaplan--Meier product limit formed from all observed gap records. Thus each
recurrent gap has equal weight, unlike \code{\link[=wc_surv]{wc_surv()}}, which weights each
subject's eligible gaps by \verb{1 / m_i*}.
}
\details{
At a gap time \code{t}, PSH uses \verb{R(t) = sum(i,j) I(Y_ij >= t)} and
\verb{dN(t) = sum(i,j) I(Y_ij = t, delta_ij = 1)}, then applies
\verb{prod(u <= t) \{1 - dN(u) / R(u)\}}. The renewal/IID interpretation of this
estimator differs from the marginal recurrent gap-time estimand of
Wang--Chang.
}
\examples{
dat <- data.frame(
  id = rep(1:3, each = 3), episode = rep(1:3, 3),
  gap = c(2, 3, 4, 1, 4, 3, 2, 2, 5),
  status = rep(c(1, 1, 0), 3)
)
psh_surv(dat, episode = "episode")

}
\references{
Pena EA, Strawderman RL, Hollander M (2001). Nonparametric estimation with
recurrent event data. \emph{Journal of the American Statistical Association},
96, 1299-1315. \doi{10.1198/016214501753381887}.
}
