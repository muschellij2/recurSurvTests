% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zhao_rank_tests.R
\name{zhao_rank_simulation}
\alias{zhao_rank_simulation}
\title{Simulate Zhao et al. null-size experiments}
\usage{
zhao_rank_simulation(
  n_sim = 1000,
  n_per_group = 100,
  scenario = c("paper", "psg_200"),
  distribution = c("exponential", "weibull", "lognormal", "loglogistic"),
  heterogeneity = NULL,
  followup = NULL,
  target_events = 200,
  time_resolution = NULL,
  alpha = 0.05,
  test = c("logrank", "gehan_breslow", "peto_prentice"),
  variance_method = c("pooled_risk", "zhao_eq6"),
  alternative = c("two.sided", "greater", "less"),
  treatment_time_multiplier = 1,
  seed = NULL
)
}
\arguments{
\item{n_sim}{Number of simulated datasets.}

\item{n_per_group}{Subjects per group.}

\item{scenario}{\code{"paper"} reproduces the broad design choices in Zhao et
al.'s Section 3. \code{"psg_200"} represents a fixed 8-hour PSG recording in
minutes, calibrated for approximately \code{target_events} completed events per
participant.}

\item{distribution}{Baseline gap-time distribution used in Zhao et al.'s
Table 1.}

\item{heterogeneity}{Two endpoints of the subject multiplier \code{Z ~ U(a, b)}.}

\item{followup}{Administrative study end time.}

\item{target_events}{Target mean number of completed events per participant
for \code{scenario = "psg_200"}.}

\item{time_resolution}{Optional gap-time discretization. The PSG default is
0.5 minutes (30-second scoring epochs); leave \code{NULL} for continuous times.}

\item{alpha}{Nominal test level.}

\item{test}{Passed to \code{\link[=zhao_rank_test]{zhao_rank_test()}}.}

\item{variance_method}{Passed to \code{\link[=zhao_rank_test]{zhao_rank_test()}}.}

\item{alternative}{Passed to \code{\link[=zhao_rank_test]{zhao_rank_test()}}. Zhao et al.'s Tables 2--3
use the one-sided \code{"less"} alternative when \code{group = "treatment"} has
longer gaps (a lower recurrent-event hazard).}

\item{treatment_time_multiplier}{Multiplicative factor applied to treatment
gap times. The default \code{1} generates the null; values above one generate
longer treatment gaps for power simulations.}

\item{seed}{Optional random seed.}
}
\value{
A list containing the empirical rejection rate, its Monte-Carlo
standard error, normal-score moments, p-values, and simulation settings.
}
\description{
Generates recurrent gap-time data under the simulation design in Section 3
of Zhao et al. (2020) and repeatedly applies \code{\link[=zhao_rank_test]{zhao_rank_test()}}. This is a
compact, reproducible diagnostic for nominal type-I-error calibration; it is
not intended to reproduce the paper's 100,000-replicate tables during a
routine package test.
}
\examples{
\donttest{
zhao_rank_simulation(n_sim = 100, n_per_group = 50, seed = 1)
}

}
\references{
Zhao et al. (2020), Section 3 and Tables 1--3.
}
