% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zhao_rank_tests.R
\name{zhao_rank_test}
\alias{zhao_rank_test}
\title{Zhao et al. extended recurrent gap-time rank test}
\usage{
zhao_rank_test(
  data,
  group,
  id = "id",
  gap = "gap",
  status = "status",
  episode = NULL,
  test = c("logrank", "gehan_breslow", "peto_prentice"),
  variance_method = c("pooled_risk", "zhao_eq6"),
  alternative = c("two.sided", "greater", "less"),
  tau = Inf
)
}
\arguments{
\item{data}{A data frame in long recurrent gap-time format.}

\item{group}{Character scalar naming a subject-level two-group variable.}

\item{id, gap, status, episode}{See \code{\link[=wc_logrank]{wc_logrank()}}.}

\item{test}{Rank weight: \code{"logrank"} (LR), \code{"gehan_breslow"} (GB), or
\code{"peto_prentice"} (PP).}

\item{variance_method}{Robust variance residual. The default \code{"pooled_risk"}
uses a pooled risk-set denominator and has residuals that sum exactly to
the score. \code{"zhao_eq6"} follows the group-specific denominator printed in
Zhao et al.'s Equation 6 as a sensitivity/reproduction implementation.}

\item{alternative}{Alternative for the normal-score p-value.}

\item{tau}{Optional upper truncation time.}
}
\value{
A list with the unstandardized score \code{W}, robust variance \code{variance},
standard error, normal score \code{z}, p-value, subject residuals, and event-time
details. \code{group1} is the treatment group, determined by second appearance
among subjects; a positive score corresponds to more events in \code{group1}.
}
\description{
Tests equality of two Wang--Chang marginal recurrent gap-time survival
distributions using the extended rank-test construction of Zhao et al.
(2020).  Unlike \code{\link[=wc_logrank]{wc_logrank()}}, this function uses Zhao et al.'s adjusted
subject-level residuals (their Equation 6) to estimate the robust variance.
}
\details{
The implementation follows Equations 3, 6, and 7 of Zhao et al. (2020).
The paper presents its calculation without tied observed times. Here, tied
events are handled by aggregating their Wang--Chang weighted increments at a
common time; this is a natural extension, but is not separately derived in
that paper. Peto--Prentice weights use the pooled Wang--Chang survival
estimate \verb{exp(-cumulative hazard)}, as defined in the paper.
}
\examples{
dat <- data.frame(
  id = rep(1:4, each = 3), episode = rep(1:3, 4),
  gap = c(2, 3, 4, 1, 4, 3, 2, 2, 5, 1, 3, 4),
  status = rep(c(1, 1, 0), 4),
  arm = rep(c("A", "A", "B", "B"), each = 3)
)
zhao_rank_test(dat, group = "arm", episode = "episode")

}
\references{
Zhao Q, Zhang B, LaValley MP, Massaro JM, Lunetta KL, Chang M (2020).
Extended rank tests for analyzing recurrent event data. \emph{Statistics in
Biopharmaceutical Research}, 12(1), 90-98.
\doi{10.1080/19466315.2019.1601596}.
}
