% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recurrent_surv_statistics.R
\name{recurrent_surv_rmst_ci}
\alias{recurrent_surv_rmst_ci}
\title{Restricted mean recurrent gap time with a subject-bootstrap confidence interval}
\usage{
recurrent_surv_rmst_ci(
  data,
  estimator = c("wc", "psh"),
  id = "id",
  gap = "gap",
  status = "status",
  episode = NULL,
  tau,
  conf.level = 0.95,
  B = 999,
  seed = NULL
)
}
\arguments{
\item{data, id, gap, status, episode}{Passed to \code{\link[=wc_surv]{wc_surv()}} or \code{\link[=psh_surv]{psh_surv()}}.}

\item{estimator}{Either \code{"wc"} or \code{"psh"}.}

\item{tau}{Positive restriction time.}

\item{conf.level}{Confidence level.}

\item{B}{Number of subject bootstrap resamples; 999 is the default and 2000
or more is recommended for stable two-sided 95 percent tails.}

\item{seed}{Optional random seed.}
}
\value{
A list with the restricted mean \code{estimate}, percentile limits, fitted
curve, and bootstrap diagnostics.
}
\description{
Estimates the area under a Wang--Chang or PSH recurrent gap-time survival
curve through \code{tau}, and obtains a percentile confidence interval by
resampling complete subject histories.
}
\details{
The restricted mean gap time is \verb{integral_0^tau S(u) du}. It remains
estimable when a survival median is not reached. Resampling is by subject,
preserving recurrent-gap clustering and WC weights.
}
