% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wang_chang.R
\name{wc_surv}
\alias{wc_surv}
\title{Wang-Chang weighted-risk-set recurrent gap-time survival estimator}
\usage{
wc_surv(
  data,
  id = "id",
  gap = "gap",
  status = "status",
  episode = NULL,
  tau = Inf
)
}
\arguments{
\item{data}{A data frame in long gap-time format with one row per observed
gap.}

\item{id}{Character scalar naming the subject identifier column.}

\item{gap}{Character scalar naming the gap-duration column.}

\item{status}{Character scalar naming the event indicator column. Completed
recurrent gaps are coded \code{1}; the final censored gap is coded \code{0}.}

\item{episode}{Optional character scalar naming the within-subject episode
order column.}

\item{tau}{Optional upper truncation time for the recurrent gap distribution.}
}
\value{
A data frame with one row per distinct completed gap time and columns
\code{time}, \code{risk}, \code{dN}, \code{dLambda}, \code{surv_left}, and \code{surv}.
}
\description{
Estimates the marginal survival distribution of recurrent gap times using the
subject-weighted risk-set representation that is algebraically equivalent to
the Wang-Chang estimator and used by Luo and Huang.
}
\details{
Each subject contributes weight \verb{1 / m_i*} to each eligible recurrent gap,
where \verb{m_i*} is the number of completed gaps used by the weighted-risk-set
construction. This avoids allowing subjects with many recurrent events to
dominate the marginal gap-time survival estimate.
}
\examples{
dat <- data.frame(
  id = rep(1:3, c(3, 2, 3)),
  gap = c(2, 3, 4, 1, 5, 2, 4, 3),
  status = c(1, 1, 0, 1, 0, 1, 1, 0),
  episode = c(1, 2, 3, 1, 2, 1, 2, 3)
)
wc_surv(dat, episode = "episode")

}
\references{
Wang MC, Chang SH (1999). Nonparametric estimation of a recurrent survival
function. \emph{Journal of the American Statistical Association}, 94, 146-153.

Luo X, Huang CY (2011). Analysis of recurrent gap time data using the weighted
risk-set method and the modified within-cluster resampling method. \emph{Statistics
in Medicine}, 30, 301-311. \doi{10.1002/sim.4074}.
}
