% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recurrent_surv_quantile_ci.R
\name{recurrent_surv_quantile_ci}
\alias{recurrent_surv_quantile_ci}
\title{Confidence intervals for recurrent-survival quantiles}
\usage{
recurrent_surv_quantile_ci(
  data,
  estimator = c("wc", "psh"),
  id = "id",
  gap = "gap",
  status = "status",
  episode = NULL,
  tau = Inf,
  survival_prob = 0.5,
  conf.level = 0.95,
  B = 999,
  seed = NULL,
  keep_bootstrap = FALSE
)
}
\arguments{
\item{data}{A data frame in the long gap-time format used by \code{\link[=wc_surv]{wc_surv()}} and
\code{\link[=psh_surv]{psh_surv()}}.}

\item{estimator}{Either \code{"wc"} for Wang--Chang or \code{"psh"} for PSH/pooled KM.}

\item{id, gap, status, episode, tau}{Passed to the selected curve estimator.}

\item{survival_prob}{Survival probability defining the quantile. \code{0.5} is
the median; \code{0.25} is the 75th percentile of the gap-time distribution.}

\item{conf.level}{Confidence level in \verb{(0, 1)}.}

\item{B}{Number of subject-bootstrap resamples. The default is 999; use
2,000 or more for stable two-sided 95 percent tail limits when computation
permits.}

\item{seed}{Optional random-number seed.}

\item{keep_bootstrap}{Logical; retain bootstrap quantiles when available.}
}
\value{
A list containing the quantile \code{estimate}, its \code{lower} and \code{upper}
confidence limits, the fitted \code{curve}, method details, and bootstrap
diagnostics. An infinite estimate or upper limit means that the fitted curve
did not reach \code{survival_prob} before the available support.
}
\description{
Estimates a quantile of a \code{\link[=wc_surv]{wc_surv()}} Wang--Chang marginal gap-time survival
curve or a \code{\link[=psh_surv]{psh_surv()}} Pena--Strawderman--Hollander curve. For example,
\code{survival_prob = 0.5} estimates the median gap time,
\verb{inf \{t: S(t) <= 0.5\}}.
}
\details{
The bootstrap resamples complete participant histories and refits the curve.
It therefore preserves recurrent-gap dependence and the event-count weights
of the WC estimator. Independence-style normal curve limits are deliberately
not inverted for a quantile interval because they do not account for
within-subject clustering.

A quantile interval, a pointwise curve interval, and a simultaneous curve
band have different coverage targets. Use \code{\link[=recurrent_surv_ci]{recurrent_surv_ci()}} for the
latter two curve-level quantities.
}
\examples{
\donttest{
dat <- data.frame(
  id = rep(1:4, each = 3), episode = rep(1:3, 4),
  gap = c(2, 3, 4, 1, 4, 3, 2, 2, 5, 1, 3, 4),
  status = rep(c(1, 1, 0), 4)
)
recurrent_surv_quantile_ci(dat, estimator = "wc", episode = "episode",
                          survival_prob = 0.5, B = 999, seed = 1)
}

}
