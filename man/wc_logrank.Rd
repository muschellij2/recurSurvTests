% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/luo_huang.R
\name{wc_logrank}
\alias{wc_logrank}
\title{Luo-Huang weighted recurrent gap-time rank test}
\usage{
wc_logrank(
  data,
  group,
  id = "id",
  gap = "gap",
  status = "status",
  episode = NULL,
  rho = 0,
  tau = Inf
)
}
\arguments{
\item{data}{A data frame in long recurrent gap-time format.}

\item{group}{Character scalar naming a subject-level two-group variable.}

\item{id}{Character scalar naming the subject identifier column.}

\item{gap}{Character scalar naming the gap-duration column.}

\item{status}{Character scalar naming the event indicator column.}

\item{episode}{Optional character scalar naming the within-subject episode
order column.}

\item{rho}{Nonnegative rank-weight parameter. \code{rho = 0} gives the recurrent
log-rank analogue; \code{rho = 1} gives the recurrent Peto-Prentice analogue.}

\item{tau}{Optional upper truncation time.}
}
\value{
A list containing the test statistic, standard error, \code{z} statistic,
chi-square statistic, p-value, subject-level influence contributions, and
event-time details.
}
\description{
Implements the two-sample \verb{G_rho*} test of Luo and Huang using the
weighted-risk-set representation of recurrent gap-time data. With \code{rho = 0},
the statistic is the recurrent gap-time analogue of the ordinary log-rank
test. With \code{rho = 1}, it is an analogue of the Peto-Prentice generalized
Wilcoxon test.
}
\details{
The variance is estimated from subject-level martingale-style contributions,
retaining the within-subject dependence among recurrent gaps.

The current implementation requires exactly two groups. Group ordering is
determined by first appearance among subjects; \code{group0} and \code{group1} are
returned explicitly so the score direction is interpretable.
}
\examples{
dat <- data.frame(
  id = rep(1:4, each = 3),
  episode = rep(1:3, 4),
  gap = c(2, 3, 4, 1, 4, 3, 2, 2, 5, 1, 3, 4),
  status = rep(c(1, 1, 0), 4),
  arm = rep(c("A", "A", "B", "B"), each = 3)
)
wc_logrank(dat, group = "arm", episode = "episode", rho = 0)

}
\references{
Luo X, Huang CY (2011). Analysis of recurrent gap time data using the weighted
risk-set method and the modified within-cluster resampling method. \emph{Statistics
in Medicine}, 30, 301-311. \doi{10.1002/sim.4074}.
}
