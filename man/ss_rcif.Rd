% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sivadasan_sankaran.R
\name{ss_rcif}
\alias{ss_rcif}
\title{Sivadasan-Sankaran recurrent cumulative-incidence estimator}
\usage{
ss_rcif(
  data,
  id = "id",
  gap = "gap",
  status = "status",
  cause = "cause",
  episode = NULL,
  subject_weight = c("one", "followup"),
  cause_weighting = c("shared", "literal_eq15"),
  survival_side = c("right", "left"),
  tau = Inf
)
}
\arguments{
\item{data}{A data frame in long recurrent gap-time format.}

\item{id}{Character scalar naming the subject identifier column.}

\item{gap}{Character scalar naming the gap-duration column.}

\item{status}{Character scalar naming the event indicator column.}

\item{cause}{Character scalar naming the recurrent-event cause column. Causes
should be non-missing on completed-event rows.}

\item{episode}{Optional character scalar naming the within-subject episode
order column.}

\item{subject_weight}{Either \code{"one"}, giving each subject unit weight,
\code{"followup"}, weighting subjects by total follow-up, or a function taking a
subject-specific data frame and returning one nonnegative scalar.}

\item{cause_weighting}{Either \code{"shared"} (default), which uses the same
subject-level \verb{m_i*} denominator for pooled and cause-specific event
processes, or \code{"literal_eq15"}, which reproduces the printed cause-specific
denominator in Equation 15 of the 2023 paper.}

\item{survival_side}{Whether RCIF increments use the right-continuous
(\code{"right"}) or left-limit (\code{"left"}) survival estimate at each event time.}

\item{tau}{Optional upper truncation time.}
}
\value{
A list containing the pooled recurrent survival process, cause-specific
recurrent cumulative-incidence curves, subject-weighting metadata, and an
additivity diagnostic.
}
\description{
Implements a transparent reference version of the recurrent cumulative
incidence function (RCIF) estimator proposed by Sivadasan and Sankaran for
recurrent competing-risk gap-time data.
}
\details{
The printed Equation 15 in the 2023 \emph{Statistica} paper uses a cause-specific
denominator \code{m_il} and an indicator requiring at least two events of a cause.
Taken literally, those cause-specific increments need not sum to the pooled
event process. Therefore the default is \code{cause_weighting = "shared"}, while
\code{"literal_eq15"} is provided for literal reproduction and sensitivity
analysis.
}
\examples{
dat <- data.frame(
  id = rep(1:4, each = 3),
  episode = rep(1:3, 4),
  gap = c(2, 3, 4, 1, 4, 3, 2, 2, 5, 1, 3, 4),
  status = rep(c(1, 1, 0), 4),
  cause = c("A", "B", NA, "A", "A", NA, "B", "A", NA, "B", "B", NA)
)
ss_rcif(dat, episode = "episode")

}
\references{
Sivadasan SM, Sankaran PG (2023). Nonparametric estimation of cumulative
incidence functions of recurrent events. \emph{Statistica}, 83(1), 3-25.

Sivadasan SM, Sankaran PG (2022). A nonparametric test for comparing recurrent
cumulative incidence functions. \emph{METRON}. \doi{10.1007/s40300-022-00228-x}.
}
